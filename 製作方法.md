# GEPT Kids 英文單字練習 製作方法

> 線上版：<https://anitatsai13.github.io/gept-kids-vocab/>
> 單一自包含 HTML（約 111KB），零依賴、無框架、無 build step，GitHub Pages 即開即用。

## 檔案結構

```
gept-kids-vocab/
├── index.html   ← App 本體（資料＋程式＋樣式全內嵌）
└── README.md
```

## 資料層

- `const VOCAB_DATA=[…]`：小學英檢核心單字（分 12 頁、共 455 字），每筆含英文、中文、主題分類（動物／食物飲料／服飾配件／顏色／家庭親屬／自然風景／健康感受／購物生活…）。
- 以「頁」為練習單位：每頁可獨立跑單字卡、選擇題、拼字與錯題特訓。

## 四大練習模式

| 模式 | 做法 |
|---|---|
| 🃏 單字卡 | 雙向翻轉（英→中／中→英）、隨機抽卡、點卡朗讀 |
| ✅ 選擇題 | 四選一即時反饋；測驗完可立即「本次錯題加強特訓」 |
| ⌨️ 拼字練習 | 互動拼寫、逐字元提示、單字發音 |
| ❌ 錯題本 | 自動記錄歷史錯題（含舊資料相容），提供錯題特訓模式（選擇＋拼字） |

## 關鍵技術

- **語音**：Web Speech API（`speechSynthesis`＋`SpeechSynthesisUtterance`），標準美式發音，免任何音檔。
- **進度保存**：答題記錄與錯題本全存 `localStorage`，重開不遺失；錯題資料結構升級時保留向後相容。
- **UI**：原生 DOM＋template string，螢幕（screen）切換式導覽，行動裝置優先。

## 維護流程

1. 直接編輯 `index.html`（新增單字＝往 `VOCAB_DATA` 加頁）
2. 瀏覽器開檔即測 → commit → push → GitHub Pages 自動部署
